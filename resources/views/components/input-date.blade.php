<div class="mt-3 b-0">
    <label for="{{ $for }}" class="form-label">{{ $label }}</label><br />
    <input type="{{ $type }}" id="{{ $for }}" name="{{ $for }}" class="w-50 form-control" {{ $attributes }}>
</div>
